name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  unit-tests:
    name: Unit Tests (Fast, Mocked) - Python ${{ matrix.python-version }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13"]
        os: [ubuntu-latest, macos-latest]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: |
          uv pip install --system -e ".[dev]"

      - name: Run unit tests with coverage
        run: |
          uv run pytest tests/ -m "not integration" --cov=src --cov-report=xml --cov-report=term -v

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        if: matrix.python-version == '3.12' && matrix.os == 'ubuntu-latest'
        with:
          file: ./coverage.xml
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

  # integration-tests:
  #   name: Integration Tests (Real GitHub API)
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 15
  #   # DISABLED: Integration tests create real PRs on data-yaml/qen-test
  #   # They require write permissions and are expensive (API rate limits, 2+ min runtime).
  #   # Run manually when needed: GITHUB_TOKEN=xxx ./poe test-integration
  #   if: false  # Explicitly disabled
  #
  #   steps:
  #     - name: Check out repository
  #       uses: actions/checkout@v4
  #
  #     - name: Set up Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.12"
  #
  #     - name: Install uv
  #       uses: astral-sh/setup-uv@v4
  #
  #     - name: Install dependencies
  #       run: |
  #         uv pip install --system -e ".[dev]"
  #
  #     - name: Authenticate gh CLI
  #       run: |
  #         echo "${{ secrets.GITHUB_TOKEN }}" | gh auth login --with-token
  #
  #     - name: Run integration tests
  #       run: |
  #         uv run pytest tests/ -m integration -v
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: |
          uv pip install --system -e ".[dev]"

      - name: Run lint
        run: |
          uv run poe lint

  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Build package
        run: |
          uv build

      - name: Verify package installs
        run: |
          uv pip install --system dist/*.whl

      - name: Test installed package
        run: |
          python -c "import qen; import qenvy"
          qen --help || true

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 7
