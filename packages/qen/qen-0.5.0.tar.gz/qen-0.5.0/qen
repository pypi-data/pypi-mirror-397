#!/usr/bin/env bash
# Wrapper for running qen via uv
# Usage: ./qen <command> [args...]

set -e

if ! command -v uv &> /dev/null; then
    echo "Error: uv is required. Install it with: curl -LsSf https://astral.sh/uv/install.sh | sh"
    exit 1
fi

exec uv run qen "$@"
