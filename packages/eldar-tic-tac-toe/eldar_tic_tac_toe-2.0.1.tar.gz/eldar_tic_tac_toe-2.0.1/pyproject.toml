[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "eldar-tic-tac-toe"
version = "2.0.1"
description = "Tic Tac Toe with Hard AI, colors and tests"
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["colorama"]
authors = [{name = "Eldar"}]

[project.scripts]
tic-tac-toe = "tic_tac_toe.cli:main"
