apiVersion: v1
kind: Service
metadata:
  name: glurpc-service
  namespace: production
spec:
  selector:
    app: glurpc
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: LoadBalancer

