# coding: utf-8

"""
    Gomtm API

    The Gomtm API

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from mtmai.clients.rest.models.chatbot_bind_request_credentials import ChatbotBindRequestCredentials
from typing import Optional, Set
from typing_extensions import Self

class ChatbotBindRequest(BaseModel):
    """
    ChatbotBindRequest
    """ # noqa: E501
    platform: StrictStr = Field(description="平台类型")
    login_method: Optional[StrictStr] = Field(default='qrcode', description="登录方式", alias="loginMethod")
    credentials: Optional[ChatbotBindRequestCredentials] = None
    __properties: ClassVar[List[str]] = ["platform", "loginMethod", "credentials"]

    @field_validator('platform')
    def platform_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['napcatqq', 'wechat', 'telegram', 'discord']):
            raise ValueError("must be one of enum values ('napcatqq', 'wechat', 'telegram', 'discord')")
        return value

    @field_validator('login_method')
    def login_method_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['qrcode', 'account']):
            raise ValueError("must be one of enum values ('qrcode', 'account')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ChatbotBindRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of credentials
        if self.credentials:
            _dict['credentials'] = self.credentials.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ChatbotBindRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        # raise errors for additional fields in the input
        for _key in obj.keys():
            if _key not in cls.__properties:
                raise ValueError("Error due to additional fields (not defined in ChatbotBindRequest) in the input: " + _key)

        _obj = cls.model_validate({
            "platform": obj.get("platform"),
            "loginMethod": obj.get("loginMethod") if obj.get("loginMethod") is not None else 'qrcode',
            "credentials": ChatbotBindRequestCredentials.from_dict(obj["credentials"]) if obj.get("credentials") is not None else None
        })
        return _obj


