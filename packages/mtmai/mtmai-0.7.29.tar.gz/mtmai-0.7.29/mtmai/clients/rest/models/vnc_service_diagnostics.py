# coding: utf-8

"""
    Gomtm API

    The Gomtm API

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from mtmai.clients.rest.models.vnc_service_diagnostics_checks_inner import VNCServiceDiagnosticsChecksInner
from mtmai.clients.rest.models.vnc_service_diagnostics_environment import VNCServiceDiagnosticsEnvironment
from typing import Optional, Set
from typing_extensions import Self

class VNCServiceDiagnostics(BaseModel):
    """
    VNCServiceDiagnostics
    """ # noqa: E501
    service: StrictStr = Field(description="Service name")
    status: StrictStr = Field(description="Overall diagnostic status")
    checks: List[VNCServiceDiagnosticsChecksInner] = Field(description="Individual diagnostic checks")
    environment: VNCServiceDiagnosticsEnvironment
    recommendations: Optional[List[StrictStr]] = Field(default=None, description="Recommendations for issues found")
    __properties: ClassVar[List[str]] = ["service", "status", "checks", "environment", "recommendations"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['healthy', 'warning', 'error']):
            raise ValueError("must be one of enum values ('healthy', 'warning', 'error')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of VNCServiceDiagnostics from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in checks (list)
        _items = []
        if self.checks:
            for _item_checks in self.checks:
                if _item_checks:
                    _items.append(_item_checks.to_dict())
            _dict['checks'] = _items
        # override the default output from pydantic by calling `to_dict()` of environment
        if self.environment:
            _dict['environment'] = self.environment.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of VNCServiceDiagnostics from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        # raise errors for additional fields in the input
        for _key in obj.keys():
            if _key not in cls.__properties:
                raise ValueError("Error due to additional fields (not defined in VNCServiceDiagnostics) in the input: " + _key)

        _obj = cls.model_validate({
            "service": obj.get("service"),
            "status": obj.get("status"),
            "checks": [VNCServiceDiagnosticsChecksInner.from_dict(_item) for _item in obj["checks"]] if obj.get("checks") is not None else None,
            "environment": VNCServiceDiagnosticsEnvironment.from_dict(obj["environment"]) if obj.get("environment") is not None else None,
            "recommendations": obj.get("recommendations")
        })
        return _obj


