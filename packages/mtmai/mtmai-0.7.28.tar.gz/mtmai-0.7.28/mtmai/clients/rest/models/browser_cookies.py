# coding: utf-8

"""
    Gomtm API

    The Gomtm API

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from mtmai.clients.rest.models.browser_cookie import BrowserCookie
from typing import Optional, Set
from typing_extensions import Self

class BrowserCookies(BaseModel):
    """
    浏览器cookies信息，按域名分组
    """ # noqa: E501
    browser_id: StrictStr = Field(description="浏览器配置ID", alias="browserId")
    browser_name: StrictStr = Field(description="浏览器配置名称", alias="browserName")
    status: StrictStr = Field(description="浏览器实例状态")
    cookies_by_domain: Dict[str, List[BrowserCookie]] = Field(description="按域名分组的cookies", alias="cookiesByDomain")
    total_cookies: StrictInt = Field(description="总cookie数量", alias="totalCookies")
    domain_count: StrictInt = Field(description="域名数量", alias="domainCount")
    retrieved_at: datetime = Field(description="获取时间", alias="retrievedAt")
    __properties: ClassVar[List[str]] = ["browserId", "browserName", "status", "cookiesByDomain", "totalCookies", "domainCount", "retrievedAt"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['running', 'stopped']):
            raise ValueError("must be one of enum values ('running', 'stopped')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BrowserCookies from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each value in cookies_by_domain (dict of array)
        _field_dict_of_array = {}
        if self.cookies_by_domain:
            for _key_cookies_by_domain in self.cookies_by_domain:
                if self.cookies_by_domain[_key_cookies_by_domain] is not None:
                    _field_dict_of_array[_key_cookies_by_domain] = [
                        _item.to_dict() for _item in self.cookies_by_domain[_key_cookies_by_domain]
                    ]
            _dict['cookiesByDomain'] = _field_dict_of_array
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BrowserCookies from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        # raise errors for additional fields in the input
        for _key in obj.keys():
            if _key not in cls.__properties:
                raise ValueError("Error due to additional fields (not defined in BrowserCookies) in the input: " + _key)

        _obj = cls.model_validate({
            "browserId": obj.get("browserId"),
            "browserName": obj.get("browserName"),
            "status": obj.get("status"),
            "cookiesByDomain": dict(
                (_k,
                        [BrowserCookie.from_dict(_item) for _item in _v]
                        if _v is not None
                        else None
                )
                for _k, _v in obj.get("cookiesByDomain", {}).items()
            ),
            "totalCookies": obj.get("totalCookies"),
            "domainCount": obj.get("domainCount"),
            "retrievedAt": obj.get("retrievedAt")
        })
        return _obj


