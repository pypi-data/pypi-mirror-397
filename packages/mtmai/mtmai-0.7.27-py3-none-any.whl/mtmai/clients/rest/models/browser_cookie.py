# coding: utf-8

"""
    Gomtm API

    The Gomtm API

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class BrowserCookie(BaseModel):
    """
    浏览器cookie信息
    """ # noqa: E501
    name: StrictStr = Field(description="Cookie名称")
    value: StrictStr = Field(description="Cookie值")
    domain: StrictStr = Field(description="Cookie所属域名")
    path: StrictStr = Field(description="Cookie路径")
    expires: Optional[datetime] = Field(default=None, description="过期时间")
    http_only: Optional[StrictBool] = Field(default=None, description="是否仅HTTP访问", alias="httpOnly")
    secure: Optional[StrictBool] = Field(default=None, description="是否仅HTTPS传输")
    same_site: Optional[StrictStr] = Field(default=None, description="SameSite属性", alias="sameSite")
    size: Optional[StrictInt] = Field(default=None, description="Cookie大小（字节）")
    __properties: ClassVar[List[str]] = ["name", "value", "domain", "path", "expires", "httpOnly", "secure", "sameSite", "size"]

    @field_validator('same_site')
    def same_site_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Strict', 'Lax', 'None']):
            raise ValueError("must be one of enum values ('Strict', 'Lax', 'None')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BrowserCookie from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BrowserCookie from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        # raise errors for additional fields in the input
        for _key in obj.keys():
            if _key not in cls.__properties:
                raise ValueError("Error due to additional fields (not defined in BrowserCookie) in the input: " + _key)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "value": obj.get("value"),
            "domain": obj.get("domain"),
            "path": obj.get("path"),
            "expires": obj.get("expires"),
            "httpOnly": obj.get("httpOnly"),
            "secure": obj.get("secure"),
            "sameSite": obj.get("sameSite"),
            "size": obj.get("size")
        })
        return _obj


