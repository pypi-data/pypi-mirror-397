# coding: utf-8

"""
    Gomtm API

    The Gomtm API

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from mtmai.clients.rest.models.mcp_info_endpoints import McpInfoEndpoints
from mtmai.clients.rest.models.mcp_tool import McpTool
from typing import Optional, Set
from typing_extensions import Self

class McpInfo(BaseModel):
    """
    MCP 服务信息
    """ # noqa: E501
    name: StrictStr = Field(description="MCP 服务名称")
    version: StrictStr = Field(description="MCP 服务版本")
    description: StrictStr = Field(description="MCP 服务描述")
    base_url: StrictStr = Field(description="MCP 服务基础URL路径", alias="baseUrl")
    endpoints: McpInfoEndpoints
    status: StrictStr = Field(description="MCP 服务状态")
    tools: Optional[List[McpTool]] = Field(default=None, description="可用的 MCP 工具列表")
    __properties: ClassVar[List[str]] = ["name", "version", "description", "baseUrl", "endpoints", "status", "tools"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['running', 'stopped', 'error']):
            raise ValueError("must be one of enum values ('running', 'stopped', 'error')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of McpInfo from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of endpoints
        if self.endpoints:
            _dict['endpoints'] = self.endpoints.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in tools (list)
        _items = []
        if self.tools:
            for _item_tools in self.tools:
                if _item_tools:
                    _items.append(_item_tools.to_dict())
            _dict['tools'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of McpInfo from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        # raise errors for additional fields in the input
        for _key in obj.keys():
            if _key not in cls.__properties:
                raise ValueError("Error due to additional fields (not defined in McpInfo) in the input: " + _key)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "version": obj.get("version"),
            "description": obj.get("description"),
            "baseUrl": obj.get("baseUrl"),
            "endpoints": McpInfoEndpoints.from_dict(obj["endpoints"]) if obj.get("endpoints") is not None else None,
            "status": obj.get("status"),
            "tools": [McpTool.from_dict(_item) for _item in obj["tools"]] if obj.get("tools") is not None else None
        })
        return _obj


