# coding: utf-8

"""
    Gomtm API

    The Gomtm API

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class SetupStatusResponse(BaseModel):
    """
    SetupStatusResponse
    """ # noqa: E501
    database_configured: StrictBool = Field(description="Whether database is configured", alias="databaseConfigured")
    setup_complete: StrictBool = Field(description="Whether system setup is complete (only database configuration needed)", alias="setupComplete")
    db_ready: Optional[StrictBool] = Field(default=None, description="Whether the database connection has been validated and is ready", alias="dbReady")
    last_error: Optional[StrictStr] = Field(default=None, description="Last error message occurred during validation (if any)", alias="lastError")
    next_step: StrictStr = Field(description="Next step in the setup process", alias="nextStep")
    __properties: ClassVar[List[str]] = ["databaseConfigured", "setupComplete", "dbReady", "lastError", "nextStep"]

    @field_validator('next_step')
    def next_step_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['database', 'complete']):
            raise ValueError("must be one of enum values ('database', 'complete')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SetupStatusResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SetupStatusResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        # raise errors for additional fields in the input
        for _key in obj.keys():
            if _key not in cls.__properties:
                raise ValueError("Error due to additional fields (not defined in SetupStatusResponse) in the input: " + _key)

        _obj = cls.model_validate({
            "databaseConfigured": obj.get("databaseConfigured"),
            "setupComplete": obj.get("setupComplete"),
            "dbReady": obj.get("dbReady"),
            "lastError": obj.get("lastError"),
            "nextStep": obj.get("nextStep")
        })
        return _obj


