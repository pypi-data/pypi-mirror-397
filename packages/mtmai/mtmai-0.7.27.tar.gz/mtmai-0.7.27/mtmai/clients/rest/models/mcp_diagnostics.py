# coding: utf-8

"""
    Gomtm API

    The Gomtm API

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from mtmai.clients.rest.models.mcp_diagnostic_check import McpDiagnosticCheck
from mtmai.clients.rest.models.mcp_tool import McpTool
from typing import Optional, Set
from typing_extensions import Self

class McpDiagnostics(BaseModel):
    """
    MCP 服务诊断结果
    """ # noqa: E501
    timestamp: datetime = Field(description="诊断执行时间")
    sse_url: StrictStr = Field(description="测试的 SSE 端点URL", alias="sseUrl")
    health_url: StrictStr = Field(description="测试的健康检查端点URL", alias="healthUrl")
    status: StrictStr = Field(description="整体诊断状态")
    message: Optional[StrictStr] = Field(default=None, description="诊断结果消息")
    checks: List[McpDiagnosticCheck] = Field(description="详细的检查结果列表")
    tools: Optional[List[McpTool]] = Field(default=None, description="检测到的工具列表")
    __properties: ClassVar[List[str]] = ["timestamp", "sseUrl", "healthUrl", "status", "message", "checks", "tools"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['healthy', 'unhealthy', 'unknown']):
            raise ValueError("must be one of enum values ('healthy', 'unhealthy', 'unknown')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of McpDiagnostics from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in checks (list)
        _items = []
        if self.checks:
            for _item_checks in self.checks:
                if _item_checks:
                    _items.append(_item_checks.to_dict())
            _dict['checks'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in tools (list)
        _items = []
        if self.tools:
            for _item_tools in self.tools:
                if _item_tools:
                    _items.append(_item_tools.to_dict())
            _dict['tools'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of McpDiagnostics from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        # raise errors for additional fields in the input
        for _key in obj.keys():
            if _key not in cls.__properties:
                raise ValueError("Error due to additional fields (not defined in McpDiagnostics) in the input: " + _key)

        _obj = cls.model_validate({
            "timestamp": obj.get("timestamp"),
            "sseUrl": obj.get("sseUrl"),
            "healthUrl": obj.get("healthUrl"),
            "status": obj.get("status"),
            "message": obj.get("message"),
            "checks": [McpDiagnosticCheck.from_dict(_item) for _item in obj["checks"]] if obj.get("checks") is not None else None,
            "tools": [McpTool.from_dict(_item) for _item in obj["tools"]] if obj.get("tools") is not None else None
        })
        return _obj


