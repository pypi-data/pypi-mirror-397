[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "frozenlake-demo"
version = "0.0.3"
description = "A minimal FrozenLake environment with custom BaseEnv"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [{name = "User", email = "user@example.com"}]
dependencies = [
    "numpy>=1.21.0"
]

[project.urls]
"Homepage" = "https://github.com/example/frozenlake-demo"
