name: Testing on PR
on:
  pull_request:
    branches: ["main"]
  workflow_call:
permissions:
  contents: write
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  test-not-expensive:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v6
      - name: Install uv
        run: pipx install uv
      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          # cache: "uv"
      - name: Install dependencies
        run: uv sync --group dev
      - name: Test with pytest
        run: uv run pytest --cov=qml_essentials --dist load -m "not expensive" -n auto
