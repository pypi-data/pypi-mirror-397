[project]
name = "area-forecast-discussion"
version = "0.1.0"
description = "Python package to gather, parse, and present Area Forecast Discussions for regions in the U.S."
readme = "README.md"
authors = [
    { name = "Derrick Day", email = "brlkid@outlook.com" }
]
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
