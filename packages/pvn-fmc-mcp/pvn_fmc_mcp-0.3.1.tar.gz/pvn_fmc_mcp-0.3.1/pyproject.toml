[project]
name = "pvn-fmc-mcp"
version = "0.3.1"
description = "An MCP server exposing FMC text output via stdio"
authors = [{ name = "PVN" }]
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["mcp"]

[project.scripts]
pvn-fmc-mcp = "pvn_fmc_mcp.server:main"

[tool.setuptools]
packages = ["pvn_fmc_mcp"]
