{
  "type": "object",
  "properties": {
    "sortBy": {
      "type": "object",
      "description": "当用户明确提到对贡献率排序、按贡献率升降序、分析贡献率最低的维度时，请生成sortBy。其余场景下不需要生成sortBy。",
      "properties": {
        "field": {
          "type": "string",
          "description": "排序字段。",
          "enum": [
            "contribution"
          ]
        },
        "order": {
          "type": "string",
          "enum": [
            "ASC",
            "DESC"
          ]
        }
      }
    }
  }
}