def calculate_duration_ms(start_time: float) -> int:
    """Calculate duration in milliseconds from start time.

    Args:
        start_time (float): Start time from time.time().

    Returns:
        int: Duration in milliseconds.
    """
