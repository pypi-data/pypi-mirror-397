[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "red-panda-code"  # this will be the PyPI package name
version = "0.1.0"
description = "A Python langauge for making 2D games."
readme = "README-PYPI.md"
license = {text = "MIT"}
authors = [
    {name = "Lunar-Bytes", email = "joar@strombackfamily.com"}
]
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["redpanda"]