[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "petonzxc-latex-tools"
version = "0.2.0"
description = "Simple LaTeX table and image generation tools"
authors = [{name = "Petr Tyapkin"}]
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
include = ["latex_tools*"]

[tool.setuptools.package-dir]
latex_tools = "hw_2"