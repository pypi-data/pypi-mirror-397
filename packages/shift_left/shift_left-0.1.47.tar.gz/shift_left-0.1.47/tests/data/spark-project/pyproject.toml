[project]
name = "spark-project"
version = "0.1.0"
description = "A set of example of Spark SQL to run before migration"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyspark>=4.0.0",
]
