[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "eldar-translation-race"
version = "1.0.0"
description = "CLI Translation Race Game (Tərcümə Yarışı)"
authors = [{name = "Eldar"}]
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
translationrace = "eldar_translation_race.game:main"
