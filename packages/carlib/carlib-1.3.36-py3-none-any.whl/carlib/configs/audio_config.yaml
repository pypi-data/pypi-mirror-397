# Default Audio Tokenizer Configuration
# This file configures audio tokenization settings for dataset to CAR conversion

# Model configuration
model_name: "facebook/encodec_32khz"  # Model for audio processing
model_type: "encodec"  # Options: encodec, dac, snac

# Processing settings  
device: "cuda"  # Options: cuda, cpu, auto
max_duration: null  # Maximum duration in seconds, null for no limit
target_sample_rate: 32000  # Target sample rate in Hz
quality_threshold: 0.0  # Quality threshold for processing

# Output format
output_format: "car"  # Options: pt, car

# Advanced settings (uncomment and modify as needed)
# For high-quality processing:
# target_sample_rate: 48000  # Higher quality sample rate
# max_duration: 120.0  # Process longer clips

# Performance settings
# batch_size: 1  # Batch size for processing
# num_workers: 4  # Number of worker threads