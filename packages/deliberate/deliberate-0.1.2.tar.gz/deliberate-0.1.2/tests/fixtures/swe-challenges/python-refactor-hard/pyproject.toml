[project]
name = "payment-refactor-challenge"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pytest>=7.0.0",
]

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["test_*.py"]
