[project]
name = "algorithm-optimization-challenge"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pytest>=7.0.0",
]

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["test_*.py"]
markers = [
    "performance: marks tests as performance tests (may be slow before optimization)",
]
