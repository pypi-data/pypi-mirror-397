[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "scaffold-cli-tool"
version = "0.2.0"
description = "A simple CLI tool to scaffold Python projects."
dependencies = [
    "click",
    "jinja2",
    "rich",
    "GitPython",
]