[project]
name = "chemistryai"   # the name you want to reserve
version = "0.0.0"
description = "Reserved package name. Do not use."
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
