[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mkmsg"
version = "0.0.5"
description = "Send WhatsApp messages and Emails easily using Python"
authors = [
    { name="Mohamed" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"

dependencies = [
    "pyautogui"
]
