# # Supplemental Redshift permissions for shared Glue role
# #
# # The base Glue role is managed in infra/, but pipeline needs additional
# # Redshift permissions. This attaches a supplemental policy to the shared role.
#
# data "aws_iam_policy_document" "glue_redshift" {
#   statement {
#     sid    = "RedshiftAccess"
#     effect = "Allow"
#     actions = [
#       "redshift:DescribeClusters",
#       "redshift:GetClusterCredentials",
#       "redshift-data:ExecuteStatement",
#       "redshift-data:DescribeStatement",
#       "redshift-data:GetStatementResult"
#     ]
#     resources = ["*"]
#   }
# }
#
# resource "aws_iam_policy" "glue_redshift" {
#   name   = "${local.resource_prefix}-glue-redshift"
#   policy = data.aws_iam_policy_document.glue_redshift.json
#
#   tags = merge(local.tags, {
#     Name = "${local.resource_prefix}-glue-redshift"
#   })
# }
#
# resource "aws_iam_role_policy_attachment" "glue_redshift" {
#   role       = local.glue_role_name
#   policy_arn = aws_iam_policy.glue_redshift.arn
# }
