[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hw2-latex-generator"
version = "0.1.0"
description = "Простой функциональный генератор LaTeX для таблиц и картинок"
readme = "README.md"
authors = [{ name = "Egor Fyodorov", email = "egorfyodorovv@gmail.com" }]
requires-python = ">=3.10"
dependencies = []