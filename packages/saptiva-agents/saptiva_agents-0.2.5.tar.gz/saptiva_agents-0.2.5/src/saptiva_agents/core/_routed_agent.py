from autogen_core import RoutedAgent


class RoutedAgent(RoutedAgent):
    """
    A base class for agents that route messages to handlers based on the type of the message
    and optional matching functions.
    """
    pass
