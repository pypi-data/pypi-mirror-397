[project]
name = "kdo-test-text-writer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "build>=1.3.0",
    "mcp[cli]>=1.24.0",
    "twine>=6.2.0",
]

[project.scripts]
kdo-test-text-writer = "kdo_test_text_writer:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
