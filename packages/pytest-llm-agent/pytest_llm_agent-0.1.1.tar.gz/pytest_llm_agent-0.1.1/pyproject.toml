[project]
name = "pytest-llm-agent"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.1.3",
    "pytest>=9.0.2",
    "typer>=0.20.0",
]

[project.scripts]
pytest-llm-agent = "pytest_llm_agent.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["pytest_llm_agent"]

[dependency-groups]
dev = [
    "mypy>=1.19.0",
]
