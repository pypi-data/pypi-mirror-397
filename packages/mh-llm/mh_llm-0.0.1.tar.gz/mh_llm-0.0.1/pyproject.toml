[tool.poetry]
name = "mh_llm"
version = "0.0.1"
description = "A Metropolis-Hastings based sampling framework for LLMs."
authors = ["Max Zuo <zuo@brown.edu>"]
readme = "README.md"
license = "MIT"

[tool.poetry.dependencies]
python = "^3.10"
vllm = ">=0.11.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
