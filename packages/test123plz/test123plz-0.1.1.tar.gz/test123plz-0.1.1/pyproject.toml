[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/test123plz"]

[project]
name = "test123plz"
version = "0.1.1"
authors = [
    {name = "adam", email = "adamzidani.az@gmail.com"},
]
description = "pico comm test"
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
