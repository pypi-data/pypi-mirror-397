[project]
name = "cl0l_minecraft_lib"
version = "0.1.0"
description = "Minecraft launcher library with mod support."
readme = "README.md"
authors = [
    { name = "Александр", email = "nnjscp@gmail.com" }
]
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = [
    "requests>=2.30"
]

[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"
