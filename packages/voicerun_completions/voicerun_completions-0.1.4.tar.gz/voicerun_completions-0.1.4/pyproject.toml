[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "voicerun_completions"
version = "0.1.4"
description = "VoiceRun Completions"
authors = [
    { name = "VoiceRun" }
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "primfunctions",
    "black>=23.3.0",
    "openai==2.8.0",
    "anthropic==0.74.0",
    "google-genai==1.52.0",
]
