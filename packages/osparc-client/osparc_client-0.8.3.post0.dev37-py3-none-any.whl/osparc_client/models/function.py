# coding: utf-8

"""
    osparc.io public API

    osparc-simcore public API specifications

    The version of the OpenAPI document: 0.13.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from osparc_client.models.project_function import ProjectFunction
from osparc_client.models.python_code_function import PythonCodeFunction
from osparc_client.models.solver_function import SolverFunction
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

FUNCTION_ONE_OF_SCHEMAS = ["ProjectFunction", "PythonCodeFunction", "SolverFunction"]

class Function(BaseModel):
    """
    Function
    """
    # data type: ProjectFunction
    oneof_schema_1_validator: Optional[ProjectFunction] = None
    # data type: PythonCodeFunction
    oneof_schema_2_validator: Optional[PythonCodeFunction] = None
    # data type: SolverFunction
    oneof_schema_3_validator: Optional[SolverFunction] = None
    actual_instance: Optional[Union[ProjectFunction, PythonCodeFunction, SolverFunction]] = None
    one_of_schemas: Set[str] = { "ProjectFunction", "PythonCodeFunction", "SolverFunction" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = Function.model_construct()
        error_messages = []
        match = 0
        # validate data type: ProjectFunction
        if not isinstance(v, ProjectFunction):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ProjectFunction`")
        else:
            match += 1
        # validate data type: PythonCodeFunction
        if not isinstance(v, PythonCodeFunction):
            error_messages.append(f"Error! Input type `{type(v)}` is not `PythonCodeFunction`")
        else:
            match += 1
        # validate data type: SolverFunction
        if not isinstance(v, SolverFunction):
            error_messages.append(f"Error! Input type `{type(v)}` is not `SolverFunction`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in Function with oneOf schemas: ProjectFunction, PythonCodeFunction, SolverFunction. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in Function with oneOf schemas: ProjectFunction, PythonCodeFunction, SolverFunction. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into ProjectFunction
        try:
            instance.actual_instance = ProjectFunction.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into PythonCodeFunction
        try:
            instance.actual_instance = PythonCodeFunction.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into SolverFunction
        try:
            instance.actual_instance = SolverFunction.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into Function with oneOf schemas: ProjectFunction, PythonCodeFunction, SolverFunction. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into Function with oneOf schemas: ProjectFunction, PythonCodeFunction, SolverFunction. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], ProjectFunction, PythonCodeFunction, SolverFunction]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


