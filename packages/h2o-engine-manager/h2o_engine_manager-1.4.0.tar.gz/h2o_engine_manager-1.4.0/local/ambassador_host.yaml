apiVersion: getambassador.io/v3alpha1
kind: Host
metadata:
  name: $LOCAL_AIEM_AMBASSADOR_HOST_RESOURCE_NAME
spec:
  ambassador_id:
    - $LOCAL_AIEM_AMBASSADOR_ID
  hostname: $LOCAL_AIEM_MANAGER_EXTERNAL_HOSTNAME
  requestPolicy:
    insecure:
      action: Route