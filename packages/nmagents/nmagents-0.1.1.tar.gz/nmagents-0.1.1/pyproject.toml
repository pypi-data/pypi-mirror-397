[project]
name = "nmagents"
version = "0.1.1"
authors = [
  { name="Alex Punnen", email="your.email@example.com" },
]
description = "A Python package for No More Agents project."
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
dependencies = []
[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"
