<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <record id="xoln_fiber_service_contract_info_form_view" model="ir.ui.view">
    <field name="name">xoln.fiber.service.contract.info.form</field>
    <field name="model">xoln.fiber.service.contract.info</field>
    <field name="arch" type="xml">
      <form>
        <group>
          <group>
            <field name="phone_number"/>
          </group>
          <group>
            <field name="router_product_id" domain="[('categ_id', '=', %(router_category)d)]" options="{'no_create': True}"/>
            <!-- <field name="router_lot_id"
                   attrs="{'readonly': [('router_product_id', '=', False)]}"
                   context="{'default_product_id': router_product_id}"
                   domain="[('product_id', '=', router_product_id)]"
                   options="{'no_open': True}"
            /> -->
          </group>
          <group>
            <field name="external_id"/>
            <field name="project_id"/>
            <field name="id_order"/>
          </group>
        </group>
      </form>
    </field>
  </record>
</odoo>
