[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "demo-pack1"
version = "0.0.2"
description = "A demo Python package"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [{name = "User", email = "user@example.com"}]
dependencies = []

[project.scripts]
demo-pack1 = "demo_pack1.example:hello_world"
