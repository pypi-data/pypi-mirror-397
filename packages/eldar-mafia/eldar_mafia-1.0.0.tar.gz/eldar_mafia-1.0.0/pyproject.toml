[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "eldar-mafia"
version = "1.0.0"
description = "CLI Mafia Game (Group Moderator Game)"
authors = [{name = "Eldar"}]
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
mafia = "eldar_mafia.game:main"
