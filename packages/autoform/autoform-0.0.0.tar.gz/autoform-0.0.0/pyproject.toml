[project]
name = "autoform"
version = "0.0.0"
description = "Transformations"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "litellm>=1.80.10",
    "optree>=0.18.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
]
