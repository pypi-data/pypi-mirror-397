[project]
name = "cjk-character-count"
version = "0.60"
description = "This is a program that counts the amount of CJK characters based on Unicode ranges and Chinese encoding standards.  "
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fonttools>=4.61.1",
    "pandas>=2.3.3",
    "python-frontmatter>=1.1.0",
    "unicode-blocks-py>=17.0.0",
]
