[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "nexom"
version = "1.0"
description = "Next original module"
authors = [
    { name = "Touri Aida" }
]
readme = "README.md"

requires-python = ">=3.10.12"

dependencies = [
    "gunicorn>=23.0.0",
]

[tool.setuptools.packages.find]
where = ["."]