[project]
name = "pytex-lib"
version = "0.1.11"
description = "Decorate your functions to direct the output to a LaTeX file."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pathlib>=1.0.1",
]
