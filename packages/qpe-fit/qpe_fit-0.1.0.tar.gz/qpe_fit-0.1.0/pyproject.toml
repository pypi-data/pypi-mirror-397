[project]
name = "qpe-fit"
version = "0.1.0"
dependencies = [
    "numpy",
    "ultranest",
    "corner",
    "matplotlib",
    "kerrgeopy",
]

[project.optional-dependencies]
gpu = ["cupy-cuda12x"]

[project.scripts]
qpe-fit = "qpe_fit.ns:main"
qpe-generate = "qpe_fit.generate_timings:main"