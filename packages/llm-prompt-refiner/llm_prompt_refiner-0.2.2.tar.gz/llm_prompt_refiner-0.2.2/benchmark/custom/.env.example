# OpenAI API Configuration
# Required for running the benchmark

# Your OpenAI API key
# Get it from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Override the default model for testing
# Default: gpt-4o-mini (cost-effective)
# Alternatives: gpt-4o, gpt-4-turbo, gpt-3.5-turbo
# MODEL=gpt-4o-mini

# Optional: Override the default embedding model
# Default: text-embedding-3-small
# EMBEDDING_MODEL=text-embedding-3-small

# Optional: Override the default judge model
# Default: gpt-4o-mini (fast and cheap)
# Alternatives: gpt-4o (more accurate but expensive)
# JUDGE_MODEL=gpt-4o-mini
