- hosts: '{{ hosts_name }}'
  name: install python3.x.x
  tasks:
    - name: install python3.x.x
      install_python:
        resources_dir: "{{resource_path}}"
        python_tar: "{{python_tar}}"
        os_and_arch: "{{os_and_arch}}"
