


[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "maze-email-service"
version = "1.0.0"
description = "Reusable email service using Azure Communication Services"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
  { name="Maze Global", email="mohmmad.farooq@purplemavens.com" }
]
dependencies = [
  "azure-communication-email",
  "email-validator",
  "jinja2",
  "pydantic",
  "pydantic-settings"
]
