"""Core configuration modules for Flow SDK.

This package contains provider-agnostic configuration primitives used by the
SDK and CLI. Modules here replace legacy implementations under
`flow._internal.*` which now act as import shims for backward compatibility.
"""
