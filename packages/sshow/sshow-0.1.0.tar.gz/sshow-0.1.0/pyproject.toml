[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sshow"
version = "0.1.0"
description = "Prettify and group your ~/.ssh/config."
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [
  { name = "Chuan Peng", email = "erichuanp@gmail.com" }
]
dependencies = []

[project.scripts]
sshow = "sshow:main"