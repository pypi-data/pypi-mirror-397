[project]
name = "gr8tool"
version = "0.1.8"
description = "A grading tool for tutors of the university of Freiburg for EidP."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

