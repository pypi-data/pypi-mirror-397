stages:
  - build
  - ci
  - release
default:
  before_script:
    - env | sort
  interruptible: true
  image: nvcr.io/nvidia/pytorch:25.04.py3
  retry:
    max: 2
    when: runner_system_failure
include:
  - local: .gitlab/workflows/**.yml
