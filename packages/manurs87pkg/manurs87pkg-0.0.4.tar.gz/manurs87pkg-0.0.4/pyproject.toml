[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "manurs87pkg"
version = "0.0.4"
description = "Test package"
authors = [{name="MNO", email="xyz@gmail.com"}]
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}

[tool.setuptools]
include-package-data = true

[tool.setuptools.package-data]
manurs87pkg = ["storage/*"]
