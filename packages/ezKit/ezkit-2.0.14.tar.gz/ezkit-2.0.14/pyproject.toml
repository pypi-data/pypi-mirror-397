#
# 标准库
# import os
#
# 第三方库
# import requests
#
# 当前目录
# import my_module
# import utils
#
[tool.isort]
profile = "black"
force_sort_within_sections = false
lines_between_types = 1

# 把当前目录作为“本地应用（first-party）”
# src_paths = ["./source"]
# known_first_party = ["./source"]
