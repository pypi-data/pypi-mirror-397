[build-system]
requires = ["uv_build>=0.7.4,<0.8.0"]
build-backend = "uv_build"

[project]
name = "tradate"
version = "0.1.3"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "akshare>=1.17.80",
]

[tool.uv.build-backend.data]
data = "src/tradate/data/"
