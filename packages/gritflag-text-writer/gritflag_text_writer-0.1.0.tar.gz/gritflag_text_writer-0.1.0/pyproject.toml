[project]
name = "gritflag-text-writer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "build>=1.3.0",
    "mcp[cli]>=1.24.0",
    "twine>=6.2.0",
]

[project.scripts]
gritflag-text-writer = "gritflag_text_writer:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
