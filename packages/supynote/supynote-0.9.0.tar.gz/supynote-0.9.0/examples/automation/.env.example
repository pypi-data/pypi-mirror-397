# Supynote Automation Configuration
# Copy this file to .env and configure your settings:
#   cp .env.example .env

# =============================================================================
# SERVER SYNC CONFIGURATION (for sync_from_server.sh)
# =============================================================================

# Your Supernote Private Cloud server connection
# Format: user@hostname:/path/to/supernote_data
# This should point to the parent directory containing your email folder
#
# Example: If your files are at:
#   root@myserver.com:/root/supernote_data/you@email.com/Supernote/Note/
# Then set:
#   SUPYNOTE_REMOTE_SERVER=root@myserver.com:/root/supernote_data
#
SUPYNOTE_REMOTE_SERVER=user@your-server:/path/to/supernote_data

# Your email address used in the Private Cloud directory structure
# This is the email you configured when setting up Supernote Private Cloud
SUPYNOTE_USER_EMAIL=your-email@example.com

# =============================================================================
# LAN SYNC CONFIGURATION (for sync_from_lan.sh)
# =============================================================================

# Your Supernote device IP address on local network
# Find it with: uv run supynote find
# Uncomment and set if using sync_from_lan.sh:
# SUPYNOTE_IP=192.168.1.100

# =============================================================================
# LOCAL PATHS
# =============================================================================

# Where to store processed PDFs and markdown files (final output)
# This is what you'll use in your PKM system (Obsidian, Logseq, etc.)
SUPYNOTE_OUTPUT_DIR=$HOME/Documents/Supernote

# Temporary cache for downloaded .note files
# These are raw files that get converted to PDFs
# Safe to delete - will be re-downloaded on next sync
SUPYNOTE_CACHE_DIR=$HOME/.cache/supynote

# Optional: Directory to automatically copy markdown files to
# Useful if you want markdowns in a specific journal/notes directory
# SUPYNOTE_JOURNALS_DIR=$HOME/Documents/journals
