[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "email_function_whl"
version = "0.1.0"
description = "Simple email API client for Fabric Notebook team"
authors = [
    { name="Abhilash Ibrahimpatnam" }
]

dependencies = ["requests"]
readme = "README.md"

[tool.setuptools]
packages = ["email_function_whl"]