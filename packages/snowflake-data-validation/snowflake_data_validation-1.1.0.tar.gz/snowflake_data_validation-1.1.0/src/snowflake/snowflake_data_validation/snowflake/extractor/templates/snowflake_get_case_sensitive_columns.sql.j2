SELECT
    COLUMN_NAME,
    COLUMN_NAME != UPPER(COLUMN_NAME) AS IS_CASE_SENSITIVE
FROM
    {{ database_name }}.INFORMATION_SCHEMA.COLUMNS
WHERE
    TABLE_NAME = '{{ table_name }}'
    AND TABLE_SCHEMA = '{{ schema_name }}';
