SELECT
{% for index_column in index_column_collection %}
    "{{ index_column.name }}",
{% endfor %}
    ROW_MD5
FROM {{ database_name }}.{{ schema_name }}."ROW_MD5_{{ chunk_id }}_{{ table_id }}"
ORDER BY
{% for index_column in index_column_collection %}
    "{{ index_column.name }}"{% if not loop.last %},{% endif %}
{% endfor %}
