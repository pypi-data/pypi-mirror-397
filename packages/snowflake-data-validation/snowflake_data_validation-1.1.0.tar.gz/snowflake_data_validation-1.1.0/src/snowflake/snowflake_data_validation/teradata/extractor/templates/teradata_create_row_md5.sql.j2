CREATE VOLATILE TABLE "ROW_MD5_{{ chunk_id }}_{{ table_id }}"(
    {% for index_column in index_column_collection %}
        "{{ index_column.name }}" {{ index_column.data_type }},
    {% endfor %}
    ROW_MD5 CHAR(32)
) ON COMMIT PRESERVE ROWS;
ET;
