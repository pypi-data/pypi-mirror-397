SELECT
{% for index_column in index_column_collection %}
    "{{ index_column.name }}",
{% endfor %}
    TRIM(ROW_MD5) AS ROW_MD5
FROM "ROW_MD5_{{ chunk_id }}_{{ table_id }}"
ORDER BY
{% for index_column in index_column_collection %}
    "{{ index_column.name }}"{% if not loop.last %},{% endif %}
{% endfor %}
