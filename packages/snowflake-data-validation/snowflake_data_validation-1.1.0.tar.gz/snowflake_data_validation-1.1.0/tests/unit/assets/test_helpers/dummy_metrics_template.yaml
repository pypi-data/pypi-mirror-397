TYPE1:
  METRIC1: 
    metric_query: METRIC1("{{ col_name }}")
    metric_normalization: TO_CHAR({{ metric_query }}, 'N4')

  METRIC2:
    metric_query: METRIC2("{{ col_name }}")
    metric_normalization: TO_CHAR({{ metric_query }}, 'N4')

TYPE2:
  METRIC1:
    metric_query: METRIC1("{{ col_name }}")
    metric_normalization: TO_CHAR({{ metric_query }})
