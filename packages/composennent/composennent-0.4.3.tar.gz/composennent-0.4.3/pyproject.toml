[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "composennent"
version = "0.4.3"
description = "Composable neural network components for building models in PyTorch."
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
    {name = "Dave", email = "tad9277@gmail.com"}
]
keywords = ["deep-learning", "transformers", "pytorch", "nlp", "vision", "machine-learning"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "torch>=2.0.0",
    "tqdm>=4.65.0",
]

[project.optional-dependencies]
# NLP: pip install composennent[nlp]
nlp = [
    "sentencepiece>=0.1.99",
]
# Vision: pip install composennent[vision]
vision = [
    "torchvision>=0.15.0",
    "pillow>=9.0.0",
]
# OCR: pip install composennent[ocr]
ocr = [
    "pytesseract>=0.3.10",
]
# Development tools
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
]
# Install everything: pip install composennent[all]
all = [
    "composennent[nlp,vision,ocr,dev]",
]

[project.urls]

Repository = "https://github.com/DataOpsFusion/composennent"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
composennent = ["py.typed"]

[tool.black]
line-length = 100
target-version = ["py38", "py39", "py310", "py311"]

[tool.ruff]
line-length = 100
target-version = "py38"
select = [
    "E",
    "F",
    "W",
    "I",
]
ignore = []

[tool.mypy]
python_version = "3.8"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
disallow_incomplete_defs = false
check_untyped_defs = true
no_implicit_optional = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "--strict-markers --cov=composennent --cov-report=term-missing"
