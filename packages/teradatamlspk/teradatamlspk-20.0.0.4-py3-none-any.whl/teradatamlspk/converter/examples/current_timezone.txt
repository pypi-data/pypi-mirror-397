PySpark code

>>> df.select(f.current_timezone().alias("curr_tz")).show()
+-------------------+
|            curr_tz|
+-------------------+
|America/Los_Angeles|
+-------------------+

teradatamlspk code

>>> df.select(f.current_timezone().alias("curr_tz")).show()
+-------+
|curr_tz|
+-------+
| -08:00|
+-------+