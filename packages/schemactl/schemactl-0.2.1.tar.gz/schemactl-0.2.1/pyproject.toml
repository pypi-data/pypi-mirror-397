[tool.poetry]
name = "schemactl"
version = "0.2.1"
description = "next‑generation database migration tool"
authors = ["Teams <teams@dotlabs.dev>"]
license = "MIT"
readme = "README.md"
packages = [{include = "schemactl"}]

[tool.poetry.dependencies]
python = "^3.12"
click = "^8.3.1"
rich = "^14.2.0"
python-dotenv = "^1.2.1"
alembic = "^1.17.2"
sqlalchemy = "^2.0.45"
asyncpg = "^0.31.0"
greenlet = "^3.3.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.2.2"
pytest-asyncio = "^0.23.7"

[tool.poetry.scripts]
schemactl = "schemactl.cli:cli"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
