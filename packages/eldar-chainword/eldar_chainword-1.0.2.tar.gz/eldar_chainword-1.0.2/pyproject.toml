[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "eldar-chainword"
version = "1.0.2"
description = "CLI Chain Word Game (Şəhər-Şəhər style)"
authors = [{name = "Eldar"}]
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
chainword = "eldar_chainword.game:main"
