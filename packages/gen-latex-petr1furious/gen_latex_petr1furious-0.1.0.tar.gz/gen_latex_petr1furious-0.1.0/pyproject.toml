[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gen-latex-petr1furious"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["typer>=0.20.0"]

[project.scripts]
latexgen = "gen_latex.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["gen_latex"]
