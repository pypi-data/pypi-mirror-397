[project]
name = "ezmsg-panel"
description = "Panel patterns for ezmsg systems"
authors = [
  { name = "Griffin Milsap", email = "griffin.milsap@gmail.com" },
]
license = "MIT"
readme = "README.md"
requires-python = ">=3.10.15,<3.13"
dynamic = ["version"]
dependencies = [
  "ezmsg>=3.6.0",
  "panel>=1.5.3",
]

[project.optional-dependencies]
sigproc = [
    "ezmsg-sigproc>=1.8.2,<2.0",
]

[dependency-groups]
dev = [
  "pre-commit>=4.0.0",
  {include-group = "lint"},
  {include-group = "test"},
]
lint = [
  "ruff",
]
test = [
  "pytest>=8.0.0",
  "pytest-asyncio>=0.24.0",
]
docs = [
  "sphinx>=8.0",
  "pydata-sphinx-theme",
  "sphinx-autodoc-typehints",
  "sphinx-copybutton",
  "myst-parser",
]

# Uncomment if your package provides CLI commands
# [project.scripts]
# ezmsg-panel = "ezmsg.panel.main:main"

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "vcs"

[tool.hatch.build.hooks.vcs]
version-file = "src/ezmsg/panel/__version__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/ezmsg"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"

[tool.ruff]
line-length = 120
target-version = "py310"
# Exclude auto-generated files
exclude = ["*/__version__.py"]

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.ruff.lint.isort]
known-first-party = ["ezmsg.panel"]
known-third-party = ["ezmsg", "ezmsg.sigproc", "panel"]

[tool.uv.sources]
# Uncomment to use development version of ezmsg from git
#ezmsg = { git = "https://github.com/ezmsg-org/ezmsg.git", branch = "dev" }
