[project]
name = "djangodjangodjango"
version = "5.0.2"
description = "Did you mean Django?"
readme = "README.md"
authors = [
    { name = "SPWG", email = "sales@shitpostwg.fun" }
]
requires-python = ">=3.10"
dependencies = ["Django==5.0.2"]
license = "BSD-3-Clause"

[build-system]
requires = ["uv_build>=0.9.5,<0.10.0"]
build-backend = "uv_build"