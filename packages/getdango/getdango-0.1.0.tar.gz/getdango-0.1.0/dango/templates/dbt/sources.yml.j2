# Auto-generated by Dango on {{ generated_at }}
# This file documents the raw data tables loaded from {{ source_name }} ({{ source_type }})
#
# EDITING THIS FILE:
# - You can edit descriptions and add tests/metadata
# - Update table/column descriptions as you learn about the data
# - For CSV sources: regeneration notes should be in .dango/sources.yml
# - For API sources: connection details should be in .dango/sources.yml
#
# DOCUMENTATION ARCHITECTURE:
# - .dango/sources.yml → operational (how to get data)
# - dbt/models/staging/sources_{{ source_name }}.yml → analytical (what data means)

version: 2

sources:
  - name: {{ source_name }}
    description: |
      Raw data from {{ source_name }} ({{ source_type }})

      This source contains operational data as-loaded from the source system.
      Data is stored in the `{{ schema_name }}` schema and should be transformed in staging models.

      Last generated: {{ generated_at }}

    schema: {{ schema_name }}

    tables:
{% for table in tables %}
      - name: {{ table.name }}
        description: |
          Raw {{ table.name }} table from {{ source_name }}

          TODO: Add description about what this table represents
          Edit this file: dbt/models/staging/sources_{{ source_name }}.yml

        columns:
{% for column in table.columns %}
          - name: {{ column.name }}
{% if column.tests %}
            tests:
{% for test in column.tests %}
              - {{ test }}
{% endfor %}
{% endif %}
{% endfor %}
{% endfor %}
