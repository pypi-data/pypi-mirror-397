# yaml-language-server: $schema=https://smithery.ai/schema/smithery.yaml
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    required: []
  commandFunction: |-
    (config) => ({ command: "uvx", args: ["kosmo-mcp"] })
