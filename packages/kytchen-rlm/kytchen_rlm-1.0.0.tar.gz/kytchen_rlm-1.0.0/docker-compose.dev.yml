version: "3.9"

services:
  kytchen-api:
    command: ["uvicorn", "kytchen.api.app:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    environment:
      KYTCHEN_SQL_ECHO: "true"
    volumes:
      - ./kytchen:/app/kytchen
