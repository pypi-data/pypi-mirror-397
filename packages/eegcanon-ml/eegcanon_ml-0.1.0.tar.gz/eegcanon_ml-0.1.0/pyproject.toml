[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "eegcanon-ml"
version = "0.1.0"
description = "Canonical EEG representation framework"
authors = [{ name = "You" }]
dependencies = [
    "numpy",
    "scipy",
    "pandas",
    "mne",
    "pydantic"
]