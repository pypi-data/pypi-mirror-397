# Nexus Server Configuration for Docker
# Copy this file to .env and customize for your deployment

# Server Configuration
PORT=8080

# Authentication (IMPORTANT: Change this in production!)
NEXUS_API_KEY=your-secure-api-key-here

# Backend Configuration
# Options: local, gcs
NEXUS_BACKEND=local

# Google Cloud Storage Backend (optional)
# Uncomment and configure if using GCS backend
#NEXUS_GCS_BUCKET_NAME=your-nexus-bucket
#NEXUS_GCS_PROJECT_ID=your-gcp-project-id
#GCS_CREDENTIALS_PATH=./gcs-credentials.json
#GOOGLE_APPLICATION_CREDENTIALS=/app/gcs-credentials.json

# Example: Generate a secure API key with:
# openssl rand -hex 32
