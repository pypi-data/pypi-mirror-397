[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mcp-word-document-typeset"
version = "1.0.0"
description = "Word文档排版MCP服务"
authors = [
    {name = "Word MCP Services", email = "support@example.com"}
]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
dependencies = [
    "python-docx>=1.1.0",
    "fastmcp>=2.8.1",
]

[project.scripts]
mcp-word-document-typeset = "mcp_word_document_typeset.main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["mcp_word_document_typeset*"]

[dependency-groups]
dev = [
    "build>=1.3.0",
    "twine>=6.1.0",
]
