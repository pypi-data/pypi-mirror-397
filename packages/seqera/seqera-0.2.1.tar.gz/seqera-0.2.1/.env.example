# Seqera AI CLI Environment Configuration
# Copy this file to .env and fill in your values

# ==============================================================================
# AUTH0 SETTINGS (Required for seqera-ai login/status/logout)
# ==============================================================================
# Domain, audience, and CLI native client ID issued by Auth0. The CLI uses these
# values to build the PKCE authorize URL and exchange codes for refresh tokens.
# Ask #platform-auth if you need the correct client ID for your environment.
#
SEQERA_AUTH0_DOMAIN=seqera-development.eu.auth0.com
SEQERA_AUTH0_AUDIENCE=platform
SEQERA_AUTH0_CLI_CLIENT_ID=3coNib1EUwfyEQ6oUNsyHhcYPiHfJWkw

# Loopback callback settings (must match the redirect URI registered in Auth0)
SEQERA_AUTH0_REDIRECT_HOST=127.0.0.1
SEQERA_AUTH0_REDIRECT_PORT=53682
SEQERA_AUTH0_REDIRECT_PATH=/callback

# Optional profile override if you want to maintain multiple tenants
# (defaults to "<domain>:<audience>")
# SEQERA_AUTH0_PROFILE=

# ==============================================================================
# BACKEND SERVER URL (Optional)
# ==============================================================================
# URL of the Seqera AI backend server that handles Claude Agent SDK integration.
SEQERA_AI_BACKEND_URL=http://localhost:8002

# ==============================================================================
# LEGACY TOKEN OVERRIDE (Optional)
# ==============================================================================
# Prefer `seqera-ai login` so tokens are stored securely in your OS keychain.
# This override is only useful for CI or temporary troubleshooting.
# SEQERA_TOKEN=
