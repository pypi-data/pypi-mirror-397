[project]
name = "py-sci-calc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "clifton.clark@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pint>=0.25.2",
    "ptpython>=3.0.32",
]

[project.scripts]
py-sci-calc = "py_sci_calc:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
