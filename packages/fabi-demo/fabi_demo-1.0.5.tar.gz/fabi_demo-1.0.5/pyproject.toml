[project]
name = "fabi-demo"
version = "1.0.5"
description = ""
authors = [
    {name = "FZV",email = "fake_email@email.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "click (>=8.3.1,<9.0.0)",
    "httpx (>=0.28.1,<0.29.0)",
    "rich (>=14.2.0,<15.0.0)"
]

# Extras Dependencies
[project.optional-dependencies]
dev = ["pre-commit"]
docs = [
    "Sphinx>=7.0",
    "sphinx_rtd_theme",
    "sphinx-copybutton",
    "sphinx-tabs",
]
tests = [
    "coverage>=6.4",
    "coveralls>=3.3",
    "pytest>=7.4",
    "pytest-cov>=4.1",
    "pytest-pep8>=1.0",
    "pydocstyle>=4.0",
    "isort>4.3",
    "check-manifest>=0.40",
]

all = ["fabi_demo[docs,tests]"]
## End extras dependencies

[build-system]
requires = ["setuptools>=67.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["fabi_demo*"]
exclude = ["tests*"]
namespaces = false

[tool.setuptools.package-data]
"fabi_demo" = ["py.typed"]

[project.scripts]
fabi-demo-cli = "fabi_demo.cli:cli"

