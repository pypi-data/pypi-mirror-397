[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nolimits"
version = "0.1.0"
description = "Arbitrary precision decimal library with string-backed storage"
authors = [{name="Callum"}]
readme = "README.md"
license = {text = "MIT"}
dependencies = []