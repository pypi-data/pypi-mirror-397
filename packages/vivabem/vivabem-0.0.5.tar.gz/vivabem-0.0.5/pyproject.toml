[project]
name = "vivabem"
version = "0.0.5"
description = "Base shared class for loading data from smartwatches collected for the VivaBem project."
authors = [{ name = "Rafael Ito", email = "ito.rafael@gmail.com" }]
readme = "README.md"

[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"
