[project]
name = "runtime-mcp"
version = "0.1.1"
description = "MCP Runtime client for accessing cloud-based MCP capabilities"
requires-python = ">=3.9"
readme = "README.md"
authors = [
  { name = "Runtime MCP" }
]
dependencies = []

[project.scripts]
runtime-mcp = "runtime.main:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
