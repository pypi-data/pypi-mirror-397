[project]
name = "uniprot-redis"
version = "1.8.3"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "docopt>=0.6.2",
    "fastapi>=0.115.6",
    "pyrediscore>=1.0.3",
    "unyprot>=0.1.0",
    "uvicorn>=0.34.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
