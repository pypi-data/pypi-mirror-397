server:
  - !channel
    address: localhost:50051
    insecure-mode: true

storages:
  - !disk-storage &main-storage
    path: !expand-path $HOME/cas

instances:
  - name: main

    services:
      - !cas
        storage: *main-storage

      - !bytestream
        storage: *main-storage
