redis:
  myredis:
    mode: pool
    host: redis
    port: 6379

storage:
  storage:
    redis:
      - myredis
    max_stream_size: 100
    read_blocking_seconds: 10

metric:
  mode: "log"

server:
  storage: storage
  host: "[::]"
  port: 50055
