[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "Notare"
version = "2025.12.16.a"
description = "A swiss knife music utility for handling music score files in the command line."
readme = "README.md"
requires-python = ">=3.9"
license = { file = "LICENSE" }
authors = [
  { name = "David Issa Mattos", email="issamattos.david@gmail.com"}
]
dependencies = [
  "fire>=0.6",
  "music21>=9.0",
]
[project.urls]
Homepage = "https://github.com/davidissamattos/notare"

[project.scripts]
notare = "notare.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
notare = []

[tool.pytest.ini_options]
testpaths = ["tests"]
