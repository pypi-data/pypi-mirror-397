[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[project]
name = "aifoundary"
version = "1.0.0"
description = "Enterprise AI Compliance: PII Detection + Secure RAG"
dependencies = [
"transformers>=4.35.0",
"sentence-transformers>=2.2.0",
"faiss-cpu>=1.7.4",
"torch>=2.0.0",
"langchain>=0.0.300",
]
[project.optional-dependencies]
dev = ["pytest", "streamlit"]
