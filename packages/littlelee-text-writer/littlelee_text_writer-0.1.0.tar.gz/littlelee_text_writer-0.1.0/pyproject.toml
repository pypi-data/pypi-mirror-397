[project]
name = "littlelee-text-writer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "littlelee2022'", email = "eun0lee@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "build>=1.3.0",
    "mcp[cli]>=1.24.0",
    "twine>=6.2.0",
]

[project.scripts]
littlelee-text-writer = "littlelee_text_writer:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
