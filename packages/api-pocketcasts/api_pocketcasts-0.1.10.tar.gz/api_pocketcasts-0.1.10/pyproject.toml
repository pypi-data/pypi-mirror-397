[project]
name = "api_pocketcasts"
version = "0.1.10"
description = "Unofficial Pocket Casts Python API client"
authors = [
	{ name = "yfhyou" }
]

requires-python = ">=3.7"
dependencies = [
	"httpx",
	"attrs",
]


[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
