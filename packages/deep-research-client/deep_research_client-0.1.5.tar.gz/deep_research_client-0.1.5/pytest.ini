[pytest]
pythonpath = src .
doctest_optionflags = NORMALIZE_WHITESPACE ELLIPSIS
addopts = -m "not integration and not llm"
asyncio_mode = auto
markers =
    llm: marks tests as LLM tests (require API keys)
    integration: marks tests as integration tests (require external services/APIs)
