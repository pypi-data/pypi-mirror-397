[project]
name = "autoops"
version = "0.2.5"
description = "AutoOps: Fully automatic AI DevOps agent with polling mode (no webhooks required)"
authors = [{ name = "AutoOps" }]
dependencies = [
  "typer",
  "requests",
  "python-dotenv"
]

[project.scripts]
autoops = "autoops.cli:main"