/* Automatically generated frame parser */
/* Generated by 0.0.1 at Fri Dec  5 01:14:49 2025. */

#pragma once

#include "frame_base.hpp"

namespace FrameParsers {

// =============================================================================
// TinyMinimal Frame Format
// =============================================================================

enum class TinyMinimalParserState : uint8_t {
    LookingForStart = 0,
    GettingMsgId = 1,
    GettingPayload = 2
};

// TinyMinimal constants
constexpr uint8_t TINY_MINIMAL_START_BYTE = 0x70;
constexpr size_t TINY_MINIMAL_HEADER_SIZE = 2;
constexpr size_t TINY_MINIMAL_FOOTER_SIZE = 0;
constexpr size_t TINY_MINIMAL_OVERHEAD = TINY_MINIMAL_HEADER_SIZE + TINY_MINIMAL_FOOTER_SIZE;

/**
 * TinyMinimal Encode Buffer
 */
class TinyMinimalEncodeBuffer {
public:
    TinyMinimalEncodeBuffer(uint8_t* data, size_t max_size)
        : data_(data), max_size_(max_size), size_(0), in_progress_(false) {}

    void reset() {
        size_ = 0;
        in_progress_ = false;
    }

    uint8_t* data() { return data_; }
    const uint8_t* data() const { return data_; }
    size_t size() const { return size_; }
    size_t max_size() const { return max_size_; }
    bool in_progress() const { return in_progress_; }

    /**
     * Encode a message into the buffer
     */
    bool encode(uint8_t msg_id, const void* msg, size_t msg_size) {
        if (in_progress_) return false;

        size_t total_size = TINY_MINIMAL_OVERHEAD + msg_size;
        if (size_ + total_size > max_size_) return false;

        uint8_t* packet_start = data_ + size_;

        // Write header
        packet_start[0] = TINY_MINIMAL_START_BYTE;
        packet_start[1] = msg_id;

        // Write message data
        if (msg_size > 0 && msg != nullptr) {
            std::memcpy(packet_start + TINY_MINIMAL_HEADER_SIZE, msg, msg_size);
        }


        size_ += total_size;
        return true;
    }

private:
    uint8_t* data_;
    size_t max_size_;
    size_t size_;
    bool in_progress_;
};

/**
 * TinyMinimal Frame Parser
 */
class TinyMinimalParser {
public:
    using MsgLengthCallback = std::function<bool(uint8_t msg_id, size_t* length)>;

    TinyMinimalParser(uint8_t* buffer, size_t buffer_size, MsgLengthCallback msg_length_cb = nullptr)
        : state_(TinyMinimalParserState::LookingForStart),
          buffer_(buffer),
          buffer_max_size_(buffer_size),
          buffer_index_(0),
          packet_size_(0),
          msg_id_(0),
          get_msg_length_(std::move(msg_length_cb)) {}

    void reset() {
        state_ = TinyMinimalParserState::LookingForStart;
        buffer_index_ = 0;
        packet_size_ = 0;
        msg_id_ = 0;
    }

    /**
     * Parse a single byte
     * Returns FrameMsgInfo with valid=true when a complete valid message is received
     */
    FrameMsgInfo parse_byte(uint8_t byte) {
        FrameMsgInfo result;

        switch (state_) {
            case TinyMinimalParserState::LookingForStart:
                if (byte == TINY_MINIMAL_START_BYTE) {
                    buffer_[0] = byte;
                    buffer_index_ = 1;
                    state_ = TinyMinimalParserState::GettingMsgId;
                }
                break;

            case TinyMinimalParserState::GettingMsgId: {
                buffer_[buffer_index_++] = byte;
                msg_id_ = byte;

                size_t msg_length = 0;
                if (get_msg_length_ && get_msg_length_(byte, &msg_length)) {
                    packet_size_ = TINY_MINIMAL_OVERHEAD + msg_length;
                    if (packet_size_ <= buffer_max_size_) {
                        state_ = TinyMinimalParserState::GettingPayload;
                    } else {
                        state_ = TinyMinimalParserState::LookingForStart;
                    }
                } else {
                    state_ = TinyMinimalParserState::LookingForStart;
                }
                break;
            }

            case TinyMinimalParserState::GettingPayload:
                if (buffer_index_ < buffer_max_size_) {
                    buffer_[buffer_index_++] = byte;
                }

                if (buffer_index_ >= packet_size_) {
                    // Use shared minimal payload validation
                    result = validate_payload_minimal(
                        buffer_, packet_size_, TINY_MINIMAL_HEADER_SIZE);
                    state_ = TinyMinimalParserState::LookingForStart;
                }
                break;
        }

        return result;
    }

private:
    TinyMinimalParserState state_;
    uint8_t* buffer_;
    size_t buffer_max_size_;
    size_t buffer_index_;
    size_t packet_size_;
    uint8_t msg_id_;
    MsgLengthCallback get_msg_length_;
};

/**
 * Encode a message with TinyMinimal format
 * Returns the number of bytes written, or 0 on failure
 */
inline size_t tiny_minimal_encode(uint8_t* buffer, size_t buffer_size,
                                           uint8_t msg_id, const uint8_t* msg, size_t msg_size) {
    size_t total_size = TINY_MINIMAL_OVERHEAD + msg_size;
    if (buffer_size < total_size) return 0;

    buffer[0] = TINY_MINIMAL_START_BYTE;

    // Use shared minimal payload encoding
    encode_payload_minimal(
        buffer + 1, msg_id, msg, msg_size);

    return total_size;
}

/**
 * Validate a complete TinyMinimal packet in a buffer
 */
inline FrameMsgInfo tiny_minimal_validate_packet(const uint8_t* buffer, size_t length) {
    if (length < TINY_MINIMAL_OVERHEAD) return FrameMsgInfo();

    if (buffer[0] != TINY_MINIMAL_START_BYTE) return FrameMsgInfo();

    // Use shared minimal payload validation
    return validate_payload_minimal(
        buffer, length, TINY_MINIMAL_HEADER_SIZE);
}

}  // namespace FrameParsers
