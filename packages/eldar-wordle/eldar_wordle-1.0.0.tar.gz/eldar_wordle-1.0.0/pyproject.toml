[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "eldar-wordle"
version = "1.0.0"
description = "CLI Wordle game (5-letter guessing game)"
authors = [{ name="Eldar" }]
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
wordle = "eldar_wordle.game:main"
