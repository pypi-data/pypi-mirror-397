[build-system]
requires = ["setuptools>=61.0", "setuptools_scm>=8.0"]
build-backend = "setuptools.build_meta"

[project]
name = "npap"
version = "0.0.1"
description = "Network Partitioning & Aggregation Package"
readme = "README.md"
authors = [{ name = "Marco Antonio Arnaiz Montero" }]
license-files = ["LICENSE"]